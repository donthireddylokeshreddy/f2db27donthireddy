extends layout 
 
block content 
  h1= title 
 
  form(id='grocaryForm', action="/resource/grocary", method="POST") 
    div.form-group 
        label.grocaryAttr(for='tomatovalue') grocary tomatovalue 
        input#tomatovalue(type='number', name='tomatovalue') 
    div.form-group 
        label.grocaryAttr(for='onionvalue') grocary onionvalue 
        input#onionvalue(type='number', name='onionvalue') 
    div.form-group 
        label.grocaryAttr(for='capsicumvaluve') grocary capsicumvaluve 
        input#capsicumvaluve(type='number', name='capsicumvaluve') 
    input(type="submit") 
 
  script. 
    document.forms['grocaryForm'].addEventListener('submit', (event) => { 
      event.preventDefault(); 
      // TODO do something here to show user that form is being submitted 
      fetch(event.target.action, { 
        method: 'POST', 
        body: new URLSearchParams(new FormData(event.target)) // event.target is the form 
      }).then((resp) => { 
        return resp.json(); // or resp.text() or whatever the server sends 
      }).then((body) => { 
        alert("Save succeded") 
        // TODO handle body 
      }).catch((error) => { 
        // TODO handle error 
        alert("Save failed " + error ) 
      }); 
    }); 